<%= link_to 'Додати новину', new_post_path , class: "btn btn-default"%><br/>
<% @news.each do |n| %>
  <%= link_to n.article, post_path(n.id) %>
  <%= sanitize(n.body) %>
  <%= link_to 'Edit', edit_post_path(n.id) %>
  <%= link_to 'Delete', post_path(n.id), method: :delete %>
  <br/>
<% end %>
